<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.healthupgrades</groupId>
    <artifactId>health-upgrades-tracker</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>health-upgrades-tracker</name>
    <description>Health Upgrades Tracker Backend</description>

    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.3</jjwt.version>
        <jacoco.version>0.8.12</jacoco.version>
        <!--
          Overrides the Testcontainers version the Boot BOM manages (1.19.7). That release ships a
          docker-java that negotiates Docker API 1.32, and Docker Engine 29 refuses anything below 1.40
          outright - "client version 1.32 is too old". The failure is a container that never starts, so
          every *IT errors before its first assertion. Staying on 1.x deliberately: Testcontainers 2.0 is
          a major release and spring-boot-testcontainers 3.2.5 is built against 1.x. See ADR-008.
        -->
        <testcontainers.version>1.21.4</testcontainers.version>
        <!--
          Not managed by spring-boot-dependencies, so the version is ours to keep correct. 7.4 is the
          last release built against logback 1.3/1.4; 8.x compiles against logback 1.5, which Boot 3.2.5
          does not ship (it resolves logback 1.4.14). Re-check this pin on any Boot upgrade. See ADR-010.
        -->
        <logstash.encoder.version>7.4</logstash.encoder.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>10.12.0</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!--
          Tracing: supplies the trace id that correlates every log line, the error body and the
          X-Trace-Id response header. The OpenTelemetry bridge is deliberate (W3C traceparent on the
          wire, vendor-neutral) - see docs/ADRs/ADR-007. No exporter: the goal is log correlation, and
          Boot composes an empty exporter list into a no-op. Version managed by spring-boot-dependencies.
        -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>

        <!--
          The JSON encoder logback-spring.xml selects under the json-logs profile. A container's log is
          read by a machine before a person, and the trace id, the level and the audit fields are only
          queryable if they are fields rather than substrings of a message. See ADR-010.
        -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>${logstash.encoder.version}</version>
        </dependency>

        <!--
          Turns what actuator already collects - request latency, error rate, JVM and connection-pool
          saturation - into something a scraper can read, alongside this application's own counters.
          A pull endpoint, so it needs no collector to exist before it is useful. Version managed by
          spring-boot-dependencies. See ADR-012.
        -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- SimpleTracer: lets a test assert on spans without booting a tracing backend. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ArchUnit: enforces the hexagonal (ports & adapters) dependency rules as unit tests -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>

        <!--
          Testcontainers: the *IT suite starts its own PostgreSQL instead of being handed one. A
          developer-supplied database can be the wrong database - anything already listening on 5432 is
          accepted silently - and the tests then validate a schema nobody meant to test. See ADR-008.
          Versions come from the testcontainers.version property above, not the Boot BOM's default;
          @ServiceConnection comes from spring-boot-testcontainers.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!--
              Runs the *IT integration tests during `verify`, keeping them out of `mvn test`.
              That split is deliberate: unit tests need no database, integration tests boot the app
              against a real PostgreSQL.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!--
              Coverage is reported, not gated. It is a diagnostic for finding untested paths; a
              percentage threshold would only invite tests written to move the number.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <!--
                      Failsafe forks its own JVM, so without a second agent the *IT suite contributes
                      nothing to the report - and the persistence adapters, which only the ITs exercise,
                      read as 0% covered. That is a report that lies in the direction of "write more
                      tests for something already tested", which is the worst direction for a diagnostic
                      to be wrong in.
                    -->
                    <execution>
                        <id>prepare-agent-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report-integration</id>
                        <goals>
                            <goal>report-integration</goal>
                        </goals>
                    </execution>
                    <!--
                      One merged view over both runs, which is what the CI summary reads. Both this and
                      the report below bind to post-integration-test, which runs *before* verify: bound
                      to verify they would sit behind failsafe:verify, so a single failing IT would skip
                      them and leave the unit-only report on disk for the summary to publish as though
                      it were merged.
                    -->
                    <execution>
                        <id>merge-all</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco.exec</include>
                                        <include>jacoco-it.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report-merged</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <outputDirectory>${project.build.directory}/site/jacoco</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
